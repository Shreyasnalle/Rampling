rules :
- id : blocking-sleep-in-route
  patterns :
    - pattern : |
        @app.$MEHTOD(...)
        def $FUNC(...) :
          ...
          time.sleep(...)
          ...
  message : "Blocking time.sleep() found in route function, this will block all the concurrent requests"
  languages : [python]
  severity : WARNING 